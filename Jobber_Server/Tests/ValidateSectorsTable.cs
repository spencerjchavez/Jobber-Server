namespace Jobber_Server.Tests;

class ValidateSectorsTable
{
    // TODO: Make test that ensures Sectors table is valid in the database. 
    /*
    CONDITIONS: 
    - For each sector, imagine the grid of all sector slices is of nxn dimensions where n is the current sector's size multiplier, 
    every other sector within the sector's cell must have size multiplier == n as well.
    */
}